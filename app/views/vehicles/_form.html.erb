<%= form_with model: @vehicle, local: true do |form| %>
  
   <% if @vehicle.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@vehicle.errors.count, "error") %> prohibited
        this Vehicle from being saved:
      </h2>
      <ul>
        <% @vehicle.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  
  <p>
    Vehicle ID:
    <%= form.text_field :VehicleID %>
  </p>
 
  <p>
    Vehicle Description:
    <%= form.text_field :VehicleDescription %>
  </p>
 
  <p>
    License Plate:
    <%= form.text_field :LicensePlate %>
  </p>

  <p>
    Weight:
    <%= form.text_field :Weight %>
  </p>

  <p>
    Capacity:
    <%= form.text_field :Capacity %>
  </p>


  <p>
    <%= form.submit %>
  </p>
<% end %>